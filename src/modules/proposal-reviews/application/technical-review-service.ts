import { randomUUID } from "node:crypto";
import { technicalCommentResolutionSchema, technicalCommentSchema, technicalContentSchema, technicalEvidenceLinkSchema, technicalReviewSchema, type TechnicalCommentDraft, type TechnicalCommentResolution, type TechnicalContentDraft, type TechnicalEvidenceLinkDraft, type TechnicalReviewDraft } from "@/modules/proposal-reviews/domain/technical-review";
export interface TechnicalReviewRepository { addContent(sectionId:string,draft:TechnicalContentDraft,actorId:string,correlationId:string):Promise<unknown>; linkEvidence(sectionId:string,draft:TechnicalEvidenceLinkDraft,actorId:string,correlationId:string):Promise<unknown>; addComment(sectionId:string,draft:TechnicalCommentDraft,actorId:string,correlationId:string):Promise<unknown>; resolveComment(sectionId:string,commentId:string,draft:TechnicalCommentResolution,actorId:string,correlationId:string):Promise<unknown>; review(sectionId:string,draft:TechnicalReviewDraft,actorId:string,correlationId:string):Promise<unknown>; }
export class TechnicalReviewService { constructor(private readonly repository:TechnicalReviewRepository){} addContent(id:string,input:unknown,actor:string,correlation:string=randomUUID()){return this.repository.addContent(id,technicalContentSchema.parse(input),actor,correlation)} linkEvidence(id:string,input:unknown,actor:string,correlation:string=randomUUID()){return this.repository.linkEvidence(id,technicalEvidenceLinkSchema.parse(input),actor,correlation)} addComment(id:string,input:unknown,actor:string,correlation:string=randomUUID()){return this.repository.addComment(id,technicalCommentSchema.parse(input),actor,correlation)} resolveComment(id:string,commentId:string,input:unknown,actor:string,correlation:string=randomUUID()){return this.repository.resolveComment(id,commentId,technicalCommentResolutionSchema.parse(input),actor,correlation)} review(id:string,input:unknown,actor:string,correlation:string=randomUUID()){return this.repository.review(id,technicalReviewSchema.parse(input),actor,correlation)} }
