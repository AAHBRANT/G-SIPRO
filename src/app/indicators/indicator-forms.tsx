"use client";import{useRouter}from"next/navigation";import{useState,type FormEvent}from"react";
type Option=Readonly<{id:string;label:string}>;async function send(url:string,body:unknown){const response=await fetch(url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),result=await response.json()as{error?:{message?:string}};if(!response.ok)throw new Error(result.error?.message??"Não foi possível concluir a operação.")}
export function IndicatorForm({users,latestDefinitions}:{users:ReadonlyArray<Option>;latestDefinitions:ReadonlyArray<Option>}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,data=new FormData(form),sourceMappings=String(data.get("sourceMappings")??"").split("\n").map(line=>line.trim()).filter(Boolean).map(line=>{const[logicalTable,field,statuses]=line.split("|").map(item=>item.trim());return{logicalTable,field,validStatuses:(statuses??"").split(",").map(item=>item.trim()).filter(Boolean)}}),dimensions=String(data.get("dimensions")??"").split("\n").map(item=>item.trim()).filter(Boolean);setBusy(true);setMessage("");try{await send("/api/indicators",{previousDefinitionId:data.get("previousDefinitionId")||undefined,code:data.get("code"),name:data.get("name"),purpose:data.get("purpose"),ownerId:data.get("ownerId"),calculationMethod:data.get("calculationMethod"),numeratorDefinition:data.get("numeratorDefinition"),denominatorDefinition:data.get("denominatorDefinition"),treatmentDefinition:data.get("treatmentDefinition"),granularity:data.get("granularity"),sourceMappings,dimensions,refreshPeriodicity:data.get("refreshPeriodicity"),refreshTime:data.get("refreshTime"),accessRule:data.get("accessRule"),rowSecurityRule:data.get("rowSecurityRule"),qualityTest:data.get("qualityTest"),qualityTolerance:data.get("qualityTolerance"),qualityOwnerId:data.get("qualityOwnerId"),effectiveFrom:data.get("effectiveFrom"),changeReason:data.get("changeReason")});setMessage("Definição versionada registrada para aprovação.");form.reset();router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Falha ao registrar indicador.")}finally{setBusy(false)}}return <form className="grid gap-4 rounded-2xl border border-border bg-surface p-6" onSubmit={submit}><div><p className="text-xs font-bold uppercase tracking-wider text-brand">BL-404/405</p><h2 className="text-xl font-bold">Definir indicador executável</h2><p className="mt-1 text-xs text-muted">A fórmula e o método são explícitos; o sistema não os deduz.</p></div><div className="grid gap-3 md:grid-cols-2"><select className="rounded-lg border border-border px-3 py-2 text-sm" name="previousDefinitionId"><option value="">Novo indicador</option>{latestDefinitions.map(item=><option key={item.id} value={item.id}>Revisar {item.label}</option>)}</select><select className="rounded-lg border border-border px-3 py-2 text-sm" name="calculationMethod" required><option value="">Método de cálculo</option><option value="PIPELINE_COUNT">Quantidade no pipeline</option><option value="PIPELINE_VALUE">Valor do pipeline</option><option value="RESULT_CONVERSION_RATE">Conversão por resultado</option><option value="FINANCIAL_CONVERSION_RATE">Conversão financeira</option><option value="AVERAGE_DISCOUNT_PERCENT">Desconto médio</option><option value="AVERAGE_MARGIN_PERCENT">Margem média</option></select><input className="rounded-lg border border-border px-3 py-2 text-sm uppercase" name="code" placeholder="Código estável" required/><input className="rounded-lg border border-border px-3 py-2 text-sm" name="name" placeholder="Nome" required/><select className="rounded-lg border border-border px-3 py-2 text-sm" name="ownerId" required>{users.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select><textarea className="min-h-20 rounded-lg border border-border px-3 py-2 text-sm md:col-span-2" name="purpose" placeholder="Finalidade" required/><textarea className="min-h-20 rounded-lg border border-border px-3 py-2 text-sm" name="numeratorDefinition" placeholder="Definição do numerador" required/><textarea className="min-h-20 rounded-lg border border-border px-3 py-2 text-sm" name="denominatorDefinition" placeholder="Definição do denominador" required/><textarea className="min-h-20 rounded-lg border border-border px-3 py-2 text-sm md:col-span-2" name="treatmentDefinition" placeholder="Tratamentos, inclusões e exclusões" required/><input className="rounded-lg border border-border px-3 py-2 text-sm md:col-span-2" name="granularity" placeholder="Evento ou entidade base da granularidade" required/><textarea className="min-h-24 rounded-lg border border-border px-3 py-2 text-sm" name="sourceMappings" placeholder={'Uma fonte por linha: tabela lógica | campo | STATUS1,STATUS2'} required/><textarea className="min-h-24 rounded-lg border border-border px-3 py-2 text-sm" name="dimensions" placeholder={'Uma dimensão por linha: tempo\ncliente\nregião'} required/><input className="rounded-lg border border-border px-3 py-2 text-sm" name="refreshPeriodicity" placeholder="Periodicidade de atualização" required/><input className="rounded-lg border border-border px-3 py-2 text-sm" name="refreshTime" placeholder="Horário ou marco de atualização" required/><textarea className="min-h-16 rounded-lg border border-border px-3 py-2 text-sm" name="accessRule" placeholder="Perfis autorizados" required/><textarea className="min-h-16 rounded-lg border border-border px-3 py-2 text-sm" name="rowSecurityRule" placeholder="Regra de segurança por linha" required/><textarea className="min-h-16 rounded-lg border border-border px-3 py-2 text-sm" name="qualityTest" placeholder="Teste de qualidade" required/><input className="rounded-lg border border-border px-3 py-2 text-sm" name="qualityTolerance" placeholder="Tolerância e bloqueio crítico" required/><select className="rounded-lg border border-border px-3 py-2 text-sm" name="qualityOwnerId" required>{users.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select><input className="rounded-lg border border-border px-3 py-2 text-sm" name="effectiveFrom" type="date" required/><input className="rounded-lg border border-border px-3 py-2 text-sm md:col-span-2" name="changeReason" placeholder="Motivo da criação ou revisão" required/></div><button className="w-fit rounded-lg bg-brand px-4 py-2 text-sm font-bold text-white" disabled={busy}>{busy?"Salvando…":"Registrar definição"}</button>{message&&<p className="text-xs text-muted" role="status">{message}</p>}</form>}
export function IndicatorApprovalForm({definitionId}:{definitionId:string}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,data=new FormData(form);setBusy(true);setMessage("");try{await send(`/api/indicators/${definitionId}/approve`,{note:data.get("note")});setMessage("Fórmula e granularidade aprovadas.");form.reset();router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Falha ao aprovar.")}finally{setBusy(false)}}return <form className="mt-3 flex flex-wrap gap-2" onSubmit={submit}><input className="min-w-64 flex-1 rounded-lg border border-border px-3 py-2 text-xs" name="note" placeholder="Nota de aprovação da fórmula e granularidade" required/><button className="rounded-lg border border-brand px-3 py-2 text-xs font-bold text-brand" disabled={busy}>{busy?"Aprovando…":"Aprovar definição"}</button>{message&&<p className="w-full text-xs text-muted" role="status">{message}</p>}</form>}
export function IndicatorCalculationForm({definitionId,requiresCurrency}:{definitionId:string;requiresCurrency:boolean}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,data=new FormData(form);setBusy(true);setMessage("");try{await send(`/api/indicators/${definitionId}/calculate`,{periodStart:data.get("periodStart"),periodEnd:data.get("periodEnd"),currency:data.get("currency")||undefined});setMessage("Snapshot calculado e auditado.");form.reset();router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Falha ao calcular.")}finally{setBusy(false)}}return <form className="mt-3 grid gap-2 rounded-xl border border-brand/30 p-3 md:grid-cols-4" onSubmit={submit}><input className="rounded-lg border border-border px-3 py-2 text-xs" name="periodStart" type="date" required/><input className="rounded-lg border border-border px-3 py-2 text-xs" name="periodEnd" type="date" required/><input className="rounded-lg border border-border px-3 py-2 text-xs uppercase" name="currency" placeholder="Moeda (ex.: BRL)" required={requiresCurrency}/><button className="rounded-lg bg-brand px-3 py-2 text-xs font-bold text-white" disabled={busy}>{busy?"Calculando…":"Calcular snapshot"}</button>{message&&<p className="text-xs text-muted md:col-span-4" role="status">{message}</p>}</form>}
export function IndicatorReconciliationForm({snapshotId}:{snapshotId:string}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,data=new FormData(form);setBusy(true);setMessage("");try{await send(`/api/indicator-snapshots/${snapshotId}/reconcile`,{note:data.get("note")});setMessage("Conciliação independente registrada.");form.reset();router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Falha ao conciliar.")}finally{setBusy(false)}}return <form className="mt-2 flex flex-wrap gap-2" onSubmit={submit}><input className="min-w-64 flex-1 rounded-lg border border-border px-3 py-2 text-xs" name="note" placeholder="Nota da conferência operacional" required/><button className="rounded-lg border border-brand px-3 py-2 text-xs font-bold text-brand" disabled={busy}>{busy?"Conciliando…":"Conciliar com operação"}</button>{message&&<p className="w-full text-xs text-muted" role="status">{message}</p>}</form>}
export function IndicatorPublicationForm({reconciliationId}:{reconciliationId:string}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,data=new FormData(form);setBusy(true);setMessage("");try{await send(`/api/indicator-reconciliations/${reconciliationId}/publish`,{note:data.get("note")});setMessage("Indicador publicado com qualidade e linhagem.");form.reset();router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Falha ao publicar.")}finally{setBusy(false)}}return <form className="mt-2 flex flex-wrap gap-2" onSubmit={submit}><input className="min-w-64 flex-1 rounded-lg border border-border px-3 py-2 text-xs" name="note" placeholder="Nota da publicação controlada" required/><button className="rounded-lg bg-brand px-3 py-2 text-xs font-bold text-white" disabled={busy}>{busy?"Publicando…":"Publicar indicador"}</button>{message&&<p className="w-full text-xs text-muted" role="status">{message}</p>}</form>}
