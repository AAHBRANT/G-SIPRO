"use client";import{useRouter}from"next/navigation";import{useState,type FormEvent}from"react";
export function DocumentEvidenceForms({documentId,canVersion,canLink}:{documentId:string;canVersion:boolean;canLink:boolean}){const router=useRouter();const[message,setMessage]=useState("");async function sendVersion(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch(`/api/documents/${documentId}/versions`,{method:"POST",body:form});const result=await response.json()as{error?:{message?:string}};setMessage(response.ok?"Arquivo e versão imutável preservados.":result.error?.message??"Falha na operação.");if(response.ok)router.refresh()}async function sendLink(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch(`/api/documents/${documentId}/links`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form.entries()))});const result=await response.json()as{error?:{message?:string}};setMessage(response.ok?"Vínculo registrado.":result.error?.message??"Falha na operação.");if(response.ok)router.refresh()}return <div className="mt-4 grid gap-3 border-t border-border pt-4">{canVersion&&<form className="grid gap-2 md:grid-cols-3" onSubmit={sendVersion}><input accept=".pdf,.doc,.docx,.xls,.xlsx,.odt,.ods,.txt,.csv" className="rounded-lg border border-border px-2 py-2 text-xs" name="file" type="file" required/><input className="rounded-lg border border-border px-2 py-2 text-xs" name="origin" required placeholder="Origem"/><button className="rounded-lg border border-brand px-3 py-2 text-xs font-bold text-brand">Importar nova versão</button></form>}{canLink&&<form className="grid gap-2 md:grid-cols-4" onSubmit={sendLink}><input className="rounded-lg border border-border px-2 py-2 text-xs" name="entityType" required placeholder="TENDER"/><input className="rounded-lg border border-border px-2 py-2 text-xs" name="entityId" required placeholder="ID da entidade"/><input className="rounded-lg border border-border px-2 py-2 text-xs" name="role" required placeholder="SOURCE"/><button className="rounded-lg border border-brand px-3 py-2 text-xs font-bold text-brand">Criar vínculo</button></form>}{message&&<span className="text-xs text-muted">{message}</span>}</div>}
