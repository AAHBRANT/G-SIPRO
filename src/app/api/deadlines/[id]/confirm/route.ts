import{NextResponse}from"next/server";import{z}from"zod";import{requirePermission}from"@/core/authorization/authorization-context";import{toApiError}from"@/core/errors/api-error";import{createRequestContext,runWithRequestContext}from"@/core/observability/request-context";import{DeadlineService}from"@/modules/deadlines/application/deadline-service";import{PrismaDeadlineRepository}from"@/modules/deadlines/infrastructure/prisma-deadline-repository";import{mapDeadlineApiError}from"@/modules/deadlines/presentation/deadline-api";
export async function POST(request:Request,route:{params:Promise<{id:string}>}){const context=createRequestContext({correlationId:request.headers.get("x-correlation-id")??undefined});return runWithRequestContext(context,async()=>{try{const authorization=await requirePermission("deadlines.confirm");const id=z.uuid().parse((await route.params).id);const data=await new DeadlineService(new PrismaDeadlineRepository()).confirm(id,await request.json(),authorization.actorId,context.correlationId);return NextResponse.json({data,correlationId:context.correlationId})}catch(error){try{mapDeadlineApiError(error)}catch(mapped){return toApiError(mapped)}}})}
