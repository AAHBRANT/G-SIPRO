import { NextResponse } from "next/server"; import { requirePermission } from "@/core/authorization/authorization-context"; import { toApiError } from "@/core/errors/api-error"; import { createRequestContext,runWithRequestContext } from "@/core/observability/request-context"; import { TechnicalReviewService } from "@/modules/proposal-reviews/application/technical-review-service"; import { PrismaTechnicalReviewRepository } from "@/modules/proposal-reviews/infrastructure/prisma-technical-review-repository"; import { mapTechnicalReviewApiError } from "@/modules/proposal-reviews/presentation/technical-review-api";
export async function POST(request:Request,{params}:{params:Promise<{sectionId:string}>}){const context=createRequestContext({correlationId:request.headers.get("x-correlation-id")??undefined});return runWithRequestContext(context,async()=>{try{const authorization=await requirePermission("proposals.technical-content.edit");const{sectionId}=await params;const data=await new TechnicalReviewService(new PrismaTechnicalReviewRepository()).addContent(sectionId,await request.json(),authorization.actorId,context.correlationId);return NextResponse.json({data,correlationId:context.correlationId},{status:201})}catch(error){try{mapTechnicalReviewApiError(error)}catch(mapped){return toApiError(mapped)}}})}
