import{NextResponse}from"next/server";import{z}from"zod";import{requirePermission}from"@/core/authorization/authorization-context";import{getDatabase}from"@/core/database/prisma";import{toApiError}from"@/core/errors/api-error";import{createRequestContext,runWithRequestContext}from"@/core/observability/request-context";import{storeDocumentFile}from"@/core/storage/document-storage";import{DocumentService}from"@/modules/documents/application/document-service";import{PrismaDocumentRepository}from"@/modules/documents/infrastructure/prisma-document-repository";import{mapDocumentApiError}from"@/modules/documents/presentation/document-api";
export async function GET(request:Request){const context=createRequestContext({correlationId:request.headers.get("x-correlation-id")??undefined});return runWithRequestContext(context,async()=>{try{await requirePermission("documents.read");const data=await getDatabase().managedDocument.findMany({include:{owner:true,versions:{orderBy:{version:"desc"}},links:true},orderBy:{createdAt:"desc"}});return NextResponse.json({data:data.map(document=>({...document,versions:document.versions.map(version=>({...version,sizeBytes:version.sizeBytes.toString()}))})),correlationId:context.correlationId})}catch(error){return toApiError(error)}})}
export async function POST(request:Request){const context=createRequestContext({correlationId:request.headers.get("x-correlation-id")??undefined});return runWithRequestContext(context,async()=>{try{const authorization=await requirePermission("documents.create");const form=await request.formData();const file=form.get("file");if(!(file instanceof File))throw new Error("Arquivo original é obrigatório.");const stored=await storeDocumentFile(file);const service=new DocumentService(new PrismaDocumentRepository());const data=await service.create({type:form.get("type"),title:form.get("title"),classification:form.get("classification"),status:"DRAFT",ownerId:form.get("ownerId")},authorization.actorId,context.correlationId);const version=await service.addVersion(data.id,{uri:stored.uri,fileHash:stored.fileHash,mimeType:stored.mimeType,sizeBytes:stored.sizeBytes,origin:z.string().trim().min(1).max(500).parse(form.get("origin"))},authorization.actorId,context.correlationId);return NextResponse.json({data:{...data,version},correlationId:context.correlationId},{status:201})}catch(error){try{mapDocumentApiError(error)}catch(mapped){return toApiError(mapped)}}})}
