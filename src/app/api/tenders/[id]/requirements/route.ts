import{NextResponse}from"next/server";import{z}from"zod";import{requirePermission}from"@/core/authorization/authorization-context";import{getDatabase}from"@/core/database/prisma";import{toApiError}from"@/core/errors/api-error";import{ResourceNotFoundError}from"@/core/errors/application-error";import{createRequestContext,runWithRequestContext}from"@/core/observability/request-context";import{RequirementService}from"@/modules/requirements/application/requirement-service";import{PrismaRequirementRepository}from"@/modules/requirements/infrastructure/prisma-requirement-repository";import{mapRequirementApiError}from"@/modules/requirements/presentation/requirement-api";
export async function GET(request:Request,route:{params:Promise<{id:string}>}){const context=createRequestContext({correlationId:request.headers.get("x-correlation-id")??undefined});return runWithRequestContext(context,async()=>{try{await requirePermission("requirements.read");const id=z.uuid().parse((await route.params).id);const data=await getDatabase().tenderRequirement.findMany({where:{tenderVersion:{tenderId:id}},include:{responsible:true,tenderVersion:true},orderBy:[{criticality:"desc"},{createdAt:"asc"}]});return NextResponse.json({data,correlationId:context.correlationId})}catch(error){return toApiError(error)}})}
export async function POST(request:Request,route:{params:Promise<{id:string}>}){const context=createRequestContext({correlationId:request.headers.get("x-correlation-id")??undefined});return runWithRequestContext(context,async()=>{try{const authorization=await requirePermission("requirements.create");const tenderId=z.uuid().parse((await route.params).id);const input=await request.json()as{tenderVersionId?:string};const source=await getDatabase().tenderVersion.findFirst({where:{id:input.tenderVersionId,tenderId},select:{id:true}});if(!source)throw new ResourceNotFoundError("A versão documental não pertence ao edital informado.");const data=await new RequirementService(new PrismaRequirementRepository()).create(input,authorization.actorId,context.correlationId);return NextResponse.json({data,correlationId:context.correlationId},{status:201})}catch(error){try{mapRequirementApiError(error)}catch(mapped){return toApiError(mapped)}}})}
