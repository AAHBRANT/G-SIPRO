"use client";import{useRouter}from"next/navigation";import{useState,type FormEvent}from"react";export function ConfirmDeadlineForm({deadlineId}:{deadlineId:string}){const router=useRouter();const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch(`/api/deadlines/${deadlineId}/confirm`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({reason:form.get("reason")})});const result=(await response.json())as{error?:{message?:string}};setMessage(response.ok?"Prazo confirmado por decisão humana.":result.error?.message??"Falha na confirmação.");if(response.ok)router.refresh()}return <form className="mt-3 flex flex-wrap gap-2" onSubmit={submit}><input className="min-w-64 flex-1 rounded-lg border border-border px-3 py-2 text-sm" name="reason" minLength={10} required placeholder="Justificativa da conferência humana"/><button className="rounded-lg border border-brand px-3 py-2 text-sm font-bold text-brand">Confirmar</button>{message&&<span className="w-full text-xs text-muted" role="status">{message}</span>}</form>}
